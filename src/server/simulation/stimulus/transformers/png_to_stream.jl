# The images for this simulation are fairly small compared to the human eye.
# The eye has roughly 126 million sensors (rods and cones).
# This sim handles only images that are 256x256 pixels.
# The pupil (32x32 pixels) will continuosly scan the image trying figure out
# The image.
